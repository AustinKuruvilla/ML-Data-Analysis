# ML Data Analysis

Data cleaning, exploratory data analysis (EDA), feature engineering, and classical ML modelling with scikit-learn.

Quickstart
----------

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m src.analysis
```
